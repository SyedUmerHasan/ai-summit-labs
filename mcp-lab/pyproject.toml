[project]
name = "mcp-lab"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "beautifulsoup4>=4.12.0",
    "markdownify>=0.13.1",
]

[project.scripts]
mcp-lab = "server:main"
